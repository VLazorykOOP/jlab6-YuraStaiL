import javax.swing.*;

public class OrbitSimulation {
    public static void main(String[] args) {
        JFrame frame = new OrbitFrame();
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setVisible(true);
    }
}
